import { styled } from "solid-styled-components";

const Edit = () => {
  return <Wrapper>Edit page</Wrapper>;
};

export default Edit;

const Wrapper = styled("section")`
  display: flex;
  gap: var(--outsidePadding);
`;
